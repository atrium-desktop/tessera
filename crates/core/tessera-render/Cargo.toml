[package]
name = "tessera-render"
description = "Compositing: client buffers to flux textures, scene to the output via flux"
readme = "README.md"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
build = "build.rs"

[dependencies]
tessera-model = { workspace = true }
flux = { workspace = true }
flux-composition-graph = { workspace = true }
flux-sys = { workspace = true }
libc.workspace = true
log.workspace = true

[package.metadata.cargo-machete]
ignored = ["flux-sys"]

[lints]
workspace = true
