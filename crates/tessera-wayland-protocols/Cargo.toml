[package]
name = "tessera-wayland-protocols"
description = "Wayland protocol interface tables generated once and shared by backend and server"
readme = "README.md"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
build = "build.rs"

[build-dependencies]
cc = "1"

[package.metadata.cargo-machete]
ignored = ["cc"]

[lints]
workspace = true
